# Financeiro — módulo server (Sprint 5)

Reservado para:

- `openCashSession()` / `closeCashSession()`
- `recordMovement()` — sangria, suprimento, estorno
- `closeOrderWithPayments()` — pagamento + movimento + comissão
- Relatórios: DRE operacional, fluxo de caixa real

**Sprint 0–4:** não implementar aqui. Relatórios read-only continuam em `src/lib/relatorios.ts` até o fechamento real existir.

Ver `docs/ARCHITECTURE.md` — espinha financeira.
